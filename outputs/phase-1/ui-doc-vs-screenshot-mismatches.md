# UI vs Documentation Mismatch Report — Phase 1
> Generated: 2026-04-06  
> Purpose: Identify discrepancies between what the CSV documentation describes and what is observable in the UI screenshots.  
> Note: In the absence of OCR-extracted screenshot text, exact button/label mismatches require manual confirmation during test execution.

---

## Mismatch Categories

- **MISMATCH** — Documentation and UI clearly differ
- **UNDOCUMENTED** — Visible in UI screenshot but absent from CSV documentation  
- **DOC-ONLY** — Present in CSV documentation but no corresponding screenshot available
- **AMBIGUOUS** — Cannot be resolved without direct UI confirmation

---

## MM-01 — Stock / Inventory Fields Visible in List but Not in Add-Part Form (DOC-ONLY)

| Item | CSV Documentation | UI Screenshot |
|------|------------------|---------------|
| **Finding** | CSV includes stock-related fields: In Stock, On Order, Total Stock, Unallocated Stock, External Stock, Variant Stock, Allocated to build orders, Allocated to sales orders, Required for build orders, Required for sales orders, Building, Scheduled to Build | No screenshot of part detail page is provided; add-part form screenshots (`Add_part.png`, `Add_part2.png`) do not show these fields |
| **Classification** | DOC-ONLY (for this phase) |
| **Likely explanation** | Stock data is read-only / computed and would appear on the Part Detail page (not the add-part form). No Part Detail screenshot is included in the source folder. |
| **Action required** | Add Part Detail page screenshot in next analysis cycle; confirm which stock columns appear in the parts list table |

---

## MM-02 — Pricing Fields Appear in CSV but Are Not Visible in Add-Part Form (DOC-ONLY)

| Item | CSV Documentation | UI Screenshot |
|------|------------------|---------------|
| **Finding** | CSV includes `Pricing min`, `Pricing max`, `Pricing updated` | These fields are not part of standard part creation form |
| **Classification** | DOC-ONLY |
| **Likely explanation** | Pricing is managed through a separate **Pricing** tab on the Part Detail page, not through the add-part creation form |
| **Action required** | Confirm with Part Detail page screenshot; pricing tab may exist but is not shown in source screenshots |

---

## MM-03 — `Starred` / `Responsible` Fields in CSV — Status Unknown in UI (AMBIGUOUS)

| Item | CSV Documentation | UI Screenshot |
|------|------------------|---------------|
| **Finding** | CSV contains `Starred` and `Responsible` fields | `Responsible` may appear in the add-part form; `Starred` is typically a user-per-part preference (star icon in parts list) |
| **Classification** | AMBIGUOUS |
| **Starred** | Likely visible as a star icon in the parts table — not confirmed in screenshot text |
| **Responsible** | May appear in the add-part form as a user/group picker — listed as inferred in observation map |
| **Action required** | Confirm `Starred` icon presence in parts list screenshot; confirm `Responsible` field label in `Add_part.png` |

---

## MM-04 — `Barcode Hash` in CSV — No Add-Part Form Field Expected (MISMATCH)

| Item | CSV Documentation | UI Screenshot |
|------|------------------|---------------|
| **Finding** | CSV exports a `Barcode Hash` field | This is a system-generated field; it would not appear in the add-part creation form as a user-editable field |
| **Classification** | MISMATCH (documentation presents as exportable, but UI form should not include this as user input) |
| **Likely explanation** | Barcode is auto-generated by InvenTree's barcode system; it may appear in the Part Detail > Barcode tab, not in the creation form |
| **Action required** | Verify barcode is not shown as an editable field in `Add_part.png`; confirm it appears only in detail/export context |

---

## MM-05 — `Image` and `Thumbnail` Fields in CSV — Image Upload Not Evidenced in Add-Part Form (AMBIGUOUS)

| Item | CSV Documentation | UI Screenshot |
|------|------------------|---------------|
| **Finding** | CSV exports `Image` and `Thumbnail` media paths; the `Add_part2.png` modal (776×759) may be an image upload step | The main add-part form (`Add_part.png`) may include an image upload control, or image upload may be deferred to the part detail page |
| **Classification** | AMBIGUOUS |
| **Possible explanation** | `Add_part2.png` small modal may represent the image selection/upload dialog within the add-part workflow |
| **Action required** | Manually confirm whether `Add_part2.png` shows an image upload dialog; check if there is a camera/image icon in `Add_part.png` |

---

## MM-06 — `Is Template` / `Variant Of` in CSV — Template/Variant System (DOC-ONLY for Add-Part)

| Item | CSV Documentation | UI Screenshot |
|------|------------------|---------------|
| **Finding** | CSV includes `Is Template` (boolean) and `Variant Of` (parent part ID), and `Revisions`/`Revision Of` | The parts list shows "Blue Widget" marked as `Is Template: TRUE` and several variant parts; the add-part form may show these but exact label/UI treatment is unconfirmed |
| **Classification** | AMBIGUOUS |
| **Likely explanation** | `Is Template` is likely a checkbox in the add-part form. `Variant Of` may be a part selector that appears when creating a variant |
| **Action required** | Confirm checkbox labels in `Add_part.png`; check whether Variant Of and Revision Of appear in the main form or only in a parts detail tab |

---

## MM-07 — `Locked` Field in CSV — Not Commonly Prominent in Add-Part Form (AMBIGUOUS)

| Item | CSV Documentation | UI Screenshot |
|------|------------------|---------------|
| **Finding** | CSV includes a `Locked` boolean field. Sample data shows "Blue Furniture Set" has `Locked: TRUE` | The "Locked" state prevents editing of the part; it may appear as a checkbox in the add-part form or only on the part detail page |
| **Classification** | AMBIGUOUS |
| **Action required** | Confirm whether a "Locked" checkbox is visible in `Add_part.png` |

---

## MM-08 — Parts Category Screenshot Shows Category Detail — Sub-Category Structure Not Confirmed (AMBIGUOUS)

| Item | CSV Documentation | UI Screenshot |
|------|------------------|---------------|
| **Finding** | CSV shows categories like "Furniture (17) > Chairs (19)" and "Furniture (17) > Tables (18)" implying a parent-child category hierarchy | `parts_category.png` shows a category detail page; whether sub-categories are visually displayed in a panel or tree is unconfirmed |
| **Classification** | AMBIGUOUS |
| **Action required** | Confirm sub-category display in `parts_category.png`; verify parent-child navigation |

---

## MM-09 — `Default Expiry` and `Minimum Stock` — Not Always Shown in Add-Part Form (DOC-ONLY risk)

| Item | CSV Documentation | UI Screenshot |
|------|------------------|---------------|
| **Finding** | CSV includes `Default Expiry` (integer — days) and `Minimum Stock`; sample data shows these are mostly set to 0 | These fields may be present in the add-part form but may require scrolling or may be in an "Advanced" section not visible in the screenshot |
| **Classification** | AMBIGUOUS |
| **Action required** | Scroll to bottom of `Add_part.png` form and confirm presence of these fields |

---

## MM-10 — `Add_part_category.png` Shows Full-Page Form — Documentation Doesn't Describe Category Icon Field (UNDOCUMENTED)

| Item | CSV Documentation | UI Screenshot |
|------|------------------|---------------|
| **Finding** | CSV does not include a Category Icon column; however, InvenTree's category management UI typically includes an icon or colour picker for categories | This element is not documented in the CSV template |
| **Classification** | UNDOCUMENTED (in CSV) |
| **Action required** | Verify whether `Add_part_category.png` includes an icon picker field |

---

## Summary Table

| ID | Description | Classification | Screenshot | CSV | Action Required |
|----|-------------|----------------|------------|-----|-----------------|
| MM-01 | Stock/inventory fields not in add-part form | DOC-ONLY | No detail page screenshot | ✅ 12 stock fields | Add detail page screenshot |
| MM-02 | Pricing fields not in add-part form | DOC-ONLY | Not visible | ✅ 3 pricing fields | Confirm pricing tab in detail page |
| MM-03 | Starred / Responsible field visibility | AMBIGUOUS | Not confirmed | ✅ Present | Manual label confirmation |
| MM-04 | Barcode Hash shown in export but not editable | MISMATCH | Not user-editable | ✅ Exported | Verify not in creation form |
| MM-05 | Image upload — possible Add_part2.png modal | AMBIGUOUS | Possible in modal | ✅ img paths exported | Confirm modal content |
| MM-06 | Is Template / Variant Of / Revision fields | AMBIGUOUS | Likely in form | ✅ Present | Confirm labels in Add_part.png |
| MM-07 | Locked field visibility | AMBIGUOUS | Not confirmed | ✅ Present | Confirm checkbox label |
| MM-08 | Sub-category tree not confirmed in screenshot | AMBIGUOUS | Not confirmed | ✅ Implied by IDs | Confirm parts_category.png layout |
| MM-09 | Default Expiry / Minimum Stock may need scroll | AMBIGUOUS | May be off-screen | ✅ Present | Scroll/confirm in Add_part.png |
| MM-10 | Category icon field undocumented in CSV | UNDOCUMENTED | Possible in form | ❌ Not in CSV | Verify Add_part_category.png |
